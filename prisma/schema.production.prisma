// TDC Market - Production Prisma Schema
// Bu dosya Vercel deploy'ında kullanılacak

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("POSTGRES_PRISMA_URL")
  directUrl = env("POSTGRES_URL_NON_POOLING")
}

// Modeller main schema.prisma ile aynı
// Vercel build sırasında bu dosya kullanılır

